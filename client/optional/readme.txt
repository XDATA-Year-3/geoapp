Place optional files in one of the subdirectories:

optional/js/*.js files are compiled into optional.min.js
optional/static/*.js files are copied to the built directory without changing.

All files should be optional, such that if they are missing they do not stop
the program from working.
